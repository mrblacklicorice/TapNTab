<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0a0322"
    tools:context=".SwipeDownToCloseTabFragment">

    <!-- Swipe Down Icon -->
    <ImageView
        android:id="@+id/swipeDownIcon"
        android:layout_width="256dp"
        android:layout_height="273dp"
        android:layout_marginTop="32dp"
        android:contentDescription="@string/swipe_down_description"
        android:src="@drawable/swipedown"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Brats Logo Background -->
    <ImageView
        android:id="@+id/bratsLogoBackground"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:alpha="0.2"
        android:contentDescription="@string/logo_description_swipe_down"
        android:scaleType="centerInside"
        android:src="@drawable/brats_logo"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/swipeDownIcon" />

    <!-- Thank You Text -->
    <TextView
        android:id="@+id/welcomeTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:text="Thank you for visiting\nBrats, Madison"
        android:textColor="#FFFFFF"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/bratsLogoBackground"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Go Back to Tab Button -->
    <Button
        android:id="@+id/goBackButton"
        android:layout_width="260dp"
        android:layout_height="50dp"
        android:layout_marginTop="32dp"
        android:backgroundTint="#FFFFFF"
        android:gravity="center"
        android:letterSpacing="0.1"
        android:text="GO BACK TO TAB"
        android:textColor="#0A0322"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/welcomeTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>
